import { Office } from "../utils/types";

export const BookingPage = ({ office }: { office: Office }) => {
  console.log(office);
  return (
    <>
      <h1>This is the booking page</h1>
    </>
  );
};
